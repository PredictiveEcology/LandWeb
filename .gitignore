## file types by extension
*.cpg
*.csv
!authorized_users.csv
!animations.csv
*.dbf
*.ini
*.log
*.ovr
*.qs
*.rds
*.tar
*.tfw
*.tif
*.xml
*.zip

docs/\.~lock\.LandWeb_module_diagram\.odp#

## cache files
*_cache
cache/
cache
cache_sqlite

## profiling files
profiles/

## app uploads
uploads/

## R and Rstudio files
/.quarto/
**/*.quarto_ipynb
rsconnect/shinyapps.io/landweb/LandWebDemo.dcf
.Rapp.history
.RData
*.Renviron
.Rhistory
.Rproj.user/

## simulation data and outputs
.checkpoint
data/
inputs
inputs/
inputs_docker/
outputs
outputs/
outputs_v2
outputs_v2/

## ignore legacy report output dirs (reports/2020, reports/2026, ...) but track
## the pipeline's Quarto sources and published PDFs.
reports/*
!reports/*.qmd
!reports/pdf/

## targets pipeline data store (keep _targets.R / _local.R, ignore the store).
## On the control node _targets is a symlink to the shared NFS store -- ignore the symlink too.
_targets
_targets/

## crew.ssh cluster config -- control-node-only, has real hostnames (see _hosts.R.example)
_hosts.R

## OAuth tokens
.httr-oauth
.httr-oauth-SUSPENDED
*.json
!renv/settings.json
## provenance manifest (report 06); committed so it travels with the repo
!_input_manifest.json

## misc OS files
Thumbs.db

## temporary files
_tmp*
tmp/

## !! Module data should be gitignored in the respective module's data dir !!
## !! inputs/ has its own .gitignore file too !!
