parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X="LCC05",shpStudyRegionX="shpStudyRegion")
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
crs(mySim$LCC05X)
crs(LCC05)
is.raster(mySim$LCC05X)
is.raster(mySim$LCC05)
is.raster(LCC05)
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
is.raster(LCC05)
Plot(LCC05)
crs(LCC05)
LCC05
help(is.raster)
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X="LCC05",shpStudyRegionX="shpStudyRegion")
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
dim(sim$LCC05X)
dim(sim$LCC05)
modelObjects(similarity())
modelObjects(sim)
moduleObjects(sim)
objects(sim)
attr(sim$LCC05X)
Plot(sim$LCC05X)
clearPlot()
Plot(sim$LCC05X)
Plot(LCC05)
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X="LCC55",shpStudyRegionX="shpStudyRegion")
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
Plot(sim$LCC05X)
Plot(mySim$LCC05X)
clearPlot()
Plot(mySim$LCC05X)
Plot(LCC05)
spades(mySim)
sim
sim$LCC05X
LCC05
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X="LCC05",shpStudyRegionX="shpStudyRegion")
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
sim$LCC05X
spades(mySim)
sim$LCC05X
sim$shpStudyRegionX
moduleDiagram()
moduleDiagram(sim)
clearPlot()
moduleDiagram(mySim)
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X="LCC05",shpStudyRegionX="shpStudyRegion")
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
objects(mySim)
mySim$LCC05X
mySim$shpStudyRegionX
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X=LCC05,shpStudyRegionX=shpStudyRegion)
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
mySim$LCC05X
clearPlot()
dev()
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X=LCC05,shpStudyRegionX=shpStudyRegion)
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
?rm
objects(mySim)
rm(LCC05,envir=mySim)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
spades(mySim)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
spades(mySim)
dev()
Plot(sim$LCC05)
Q
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
clearPlot()
Dev()
dev()
dev(4)
spades(mySim)
Plot(sim$rstFlammable)
Plot(sim$rstBurnProb)
crs(sim$rstBurnProb
)
crs(sim$LCC05)
x<-which(sim$rstFlammable == 0)
x<-which(mySim$rstFlammable == 0)
x<-which(mySim$rstFlammable[] == 0)
sim$rstBurnProb[x] <- 0
mySim$rstBurnProb[x] <- 0
Plot(mySim$rstBurnProb)
clearPlot()
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
spades(mySim)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
spades(mySim)
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X=LCC05,shpStudyRegionX=shpStudyRegion)
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
Plot(sim$rstBurnProb)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
spades(mySim)
range(sim$rstBurnProb[])
?range
range(sim$rstBurnProb[],na.rm=TRUE)
shpStudyRegion
names(shpStudyRegion)
range(shpStudyRegion$LTHRC)
q
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
spades(mySim)
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
shpStudyRegion$fireReturnInterval <- shpStudyRegion$LTHRC #because of course it does.
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X=LCC05,shpStudyRegionX=shpStudyRegion)
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
range(rstBurnProb,na.rm=TRUE)
range(rstBurnProb[],na.rm=TRUE)
shpStudyRegion$fireReturnInterval
sim$shpStudyRegion$fireReturnInterval
?rasterize
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
shpStudyRegion$fireReturnInterval <- shpStudyRegion$LTHRC #because of course it does.
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X=LCC05,shpStudyRegionX=shpStudyRegion)
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
range(sim$shpStudyRegion$pBurn)
Q
library(igraph)
library(rgdal)
library(raster)
library(magrittr)
library(SpaDES)
baseDir<-file.path("~","Dropbox","SpaDES","Data")
LCC05 <- raster(file.path(baseDir,
"LandCoverOfCanada2005_V1_4",
"LCC2005_V1_4a.tif")
)
foo<-path.expand(file.path(baseDir,"LandWEB","shp")) #somebody does not like ~s
shpStudyRegion <- readOGR(foo,"shpLandweb")
shpStudyRegion$fireReturnInterval <- shpStudyRegion$LTHRC #because of course it does.
rm(foo)
inputDir <- file.path(tempdir(), "inputs") %>% checkPath(create = TRUE)
outputDir <- file.path(tempdir(), "outputs")
times <- list(start = 0, end = 10)
parameters <- list(
# .progress = list(type = "text", interval = 1), # for a progress bar
fireNULL = list(startTime = 1,
returnInterval = 1,
doAgeMapping = FALSE,
.statsInitialTime = 1
),
initBaseMaps = list(),
fireDataPrep = list()
)
modules <- list("initBaseMaps","fireDataPrep","fireNull")
objects <- list(LCC05X=LCC05,shpStudyRegionX=shpStudyRegion)
paths <- list(
cachePath = file.path(outputDir, "cache"),
modulePath = file.path("."), #SGC: is this really a sensible default?
inputPath = inputDir,
outputPath = outputDir
)
mySim <- simInit(times = times, params = parameters, modules = modules,
objects = objects, paths = paths)
#nxtSim<-spades(mySim)
spades(mySim)
Q
